//! RateCard seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RateCard data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// RATECARD SEEDER
// ============================================================================

/// Seeder for rate_card
pub struct SeedRateCardSeeder;

impl SeedRateCardSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRateCardSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRateCardSeeder {
    fn name(&self) -> &'static str {
        "SeedRateCardSeeder"
    }

    fn order(&self) -> i32 {
        1
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if timesheet.rate_cards table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM timesheet.rate_cards")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/rate_card_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM timesheet.rate_cards")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

